services:
     bot:
          container_name: rhenium
          build:
               context: .
               dockerfile: Dockerfile
          env_file:
               - .env
          volumes:
               - ./cfg.global.yml:/rhenium/cfg.global.yml
