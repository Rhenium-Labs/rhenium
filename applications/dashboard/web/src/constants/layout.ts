/** Layout dimensions. Values match Tailwind @theme in index.css. */
export const LAYOUT = {
	/** Server icon strip width (px) */
	sidebarIconWidth: 72,
	/** Channel list panel width (px) */
	sidebarPanelWidth: 240,
} as const;
