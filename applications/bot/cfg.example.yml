developers:
     - "<user_id>"
     - "<user_id>"
     - "<user_id>"

database:
     messages:
          insert_cron: "0 * * * *" # Every hour.
          delete_cron: "0 */6 * * *" # Every 6 hours.
          ttl: 1209600000 # 14 days in milliseconds.

     reports:
          disregard_cron: "0 */12 * * *" # Every 12 hours.
